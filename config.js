var config = {};

config.db = {};

// Image search host name


config.webhost = 'https://image-search-kesi2k.c9users.io'

// MongoDB host and database name

config.db.host = 'localhost';
config.db.name = 'image_search'

module.exports = config;